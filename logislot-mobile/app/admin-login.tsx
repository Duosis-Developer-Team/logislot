import { PortalLoginScreen } from "@/components/portal-login";

/** Yönetim Paneli girişi — tenant/tesis yönetim hesapları. */
export default function AdminLogin() {
  return <PortalLoginScreen portal="admin" />;
}
