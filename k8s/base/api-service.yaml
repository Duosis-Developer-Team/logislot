apiVersion: v1
kind: Service
metadata:
  name: logislot-api
  labels:
    app.kubernetes.io/name: logislot-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: logislot-api
  ports:
    - name: http
      port: 8000
      targetPort: http
